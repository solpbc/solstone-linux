# Synthetic advisory database fixtures

Every `RUSTSEC-2099-*` identifier in this directory is synthetic test data. These
identifiers must never be copied into the real advisory policy or treated as real
RustSec advisories.
